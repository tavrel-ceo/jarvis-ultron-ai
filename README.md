# JARVIS × ULTRON AI\n\nBase consolidada: React/Vite + Express/TypeScript, três modos de raciocínio, contexto, pesquisa web opcional e API compatível com OpenAI Chat Completions.\n\n## Rodar\n`npm install` → copie `.env.example` para `.env` → configure `AI_API_KEY` → `npm run dev`.\n