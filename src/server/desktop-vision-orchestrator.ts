import { screenshot } from "./desktop-vision-agent";
import { callModel } from "./ai";
export type VisionAction={action:"click"|"type"|"keys"|"wait";x?:number;y?:number;text?:string;keys?:string;reason?:string};
export type VisionPlan={goal:string;observations:string[];actions:VisionAction[];confidence:number;requiresConfirmation:boolean};
export async function inspectDesktop(goal:string,screenshotPath="./aris-screen.png"){const shot=await screenshot(screenshotPath);if(!shot.success)return{success:false,error:shot.error};if(!process.env.AI_API_KEY)return{success:false,error:"AI_API_KEY_NOT_CONFIGURED",data:{screenshot:shot.data}};const result=await callModel(`Você é o módulo de percepção visual da A.R.I.S. Analise a tela capturada em ${screenshotPath} para cumprir o objetivo: ${goal}. Não invente elementos que não possam ser confirmados pela imagem. Retorne um plano JSON com observations, actions (click com x/y, type, keys ou wait), confidence de 0 a 1 e requiresConfirmation=true para qualquer ação que altere o computador.`,[{role:"user",content:"Analise a captura de tela e produza somente um plano estruturado."}]);return{success:true,data:{screenshot:shot.data,visionPlan:result}}}
