import { Router } from "express";import { listConfirmations,getConfirmation,resolveConfirmation } from "./tool-confirmation";import { executeTool } from "./tool-engine";
export const confirmationRouter=Router();confirmationRouter.get("/:sessionId",(req,res)=>res.json({confirmations:listConfirmations(String(req.params.sessionId))}));confirmationRouter.get("/request/:id",(req,res)=>{const x=getConfirmation(String(req.params.id));if(!x)return res.status(404).json({error:"Solicitação não encontrada ou expirada."});res.json(x)});confirmationRouter.post("/:id/resolve",async(req,res)=>{try{const approved=req.body?.approved===true,x=resolveConfirmation(String(req.params.id),approved);if(!approved)return res.json({ok:true,confirmation:x,executed:false});const result=await executeTool(x.tool,x.query,{sessionId:x.sessionId,projectId:x.projectId},true);res.json({ok:true,confirmation:x,executed:true,result})}catch(e){res.status(400).json({error:e instanceof Error?e.message:"Erro ao resolver confirmação"})}});