import { callModel } from "./ai";
import { runTaskPlan,type TaskPlan,type TaskRun } from "./task-orchestrator";
export type RecoveryResult={success:boolean;attempts:number;history:any[];finalRun?:TaskRun;error?:string};
const allowed=new Set(["browser.navigate","browser.extract","browser.task","desktop.visual_loop","github.status","github.diff","github.inspect_repository","github.commit"]);
export async function recoverTask(goal:string,plan:TaskPlan,failedRun:TaskRun,maxAttempts=2):Promise<RecoveryResult>{const history:any[]=[{type:"initial",run:failedRun}];let current=plan;for(let attempt=1;attempt<=Math.min(3,Math.max(0,maxAttempts));attempt++){if(!process.env.AI_API_KEY)return{success:false,attempts:attempt-1,history,error:"AI_API_KEY_NOT_CONFIGURED"};const raw=await callModel(`Você é o Recovery Agent da A.R.I.S. O plano abaixo falhou. Diagnostique a falha e proponha uma alternativa conservadora. Só use operações permitidas: browser.navigate, browser.extract, browser.task, desktop.visual_loop, github.status, github.diff, github.inspect_repository, github.commit. Não repita exatamente a etapa que falhou se houver uma alternativa. Não invente ferramentas. Retorne JSON {goal,steps,recoveryReason}. Plano: ${JSON.stringify(current).slice(0,12000)} Falha: ${JSON.stringify(failedRun).slice(0,12000)}`,[{role:"user",content:"Produza somente o plano JSON de recuperação."}]);let next:any;try{next=typeof raw==="string"?JSON.parse(raw):raw}catch{return{success:false,attempts:attempt,history,error:"RECOVERY_PLAN_PARSE_FAILED"}};if(!next?.steps||!Array.isArray(next.steps))return{success:false,attempts:attempt,history,error:"RECOVERY_PLAN_INVALID"};for(const s of next.steps){if(!allowed.has(`${s.agent}.${s.action}`))return{success:false,attempts:attempt,history,error:`RECOVERY_ACTION_NOT_ALLOWED:${s.agent}.${s.action}`};if(["task","visual_loop","commit"].includes(s.action))s.requiresConfirmation=true}current={goal:next.goal||goal,steps:next.steps};const run=await runTaskPlan(current,async step=>!step.requiresConfirmation);history.push({type:"recovery",attempt,reason:next.recoveryReason,plan:current,run});if(run.success)return{success:true,attempts:attempt,history,finalRun:run};current={goal,steps:current.steps}}return{success:false,attempts:Math.min(3,Math.max(0,maxAttempts)),history,error:"RECOVERY_ATTEMPTS_EXHAUSTED"}}
