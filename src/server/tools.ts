import{webSearch}from"./search";

function arithmetic(input:string){const s=input.replace(/,/g,".").replace(/[^0-9+\-*/().%\s]/g,"").trim();if(!s||!/^[0-9+\-*/().%\s]+$/.test(s))return null;try{const tokens=s.match(/\d+(?:\.\d+)?|[()+\-*/%]/g)||[];if(tokens.join("")!==s.replace(/\s+/g,""))return null;const values:number[]=[];const ops:string[]=[];const prec=(o:string)=>o==="+"||o==="-"?1:o==="*"||o==="/"||o==="%"?2:0;const apply=()=>{const o=ops.pop();const b=values.pop();const a=values.pop();if(o===undefined||a===undefined||b===undefined)throw Error("expressão inválida");if(o==="+")values.push(a+b);else if(o==="-")values.push(a-b);else if(o==="*")values.push(a*b);else if(o==="/"){if(b===0)throw Error("divisão por zero");values.push(a/b)}else values.push(a%b)};let prev="op";for(let i=0;i<tokens.length;i++){const t=tokens[i];if(/^\d/.test(t)){values.push(Number(t));prev="num"}else if(t==="("){ops.push(t);prev="op"}else if(t==="){while(ops.at(-1)!=="("&&ops.length)apply();if(ops.pop()!=="(")throw Error("parênteses");prev="num"}else{if(t==="-"&&prev==="op")values.push(0);while(ops.length&&ops.at(-1)!=="("&&prec(ops.at(-1)!)>=prec(t))apply();ops.push(t);prev="op"}}while(ops.length)apply();return values.length===1&&Number.isFinite(values[0])?values[0]:null}catch{return null}}

export async function runTools(query:string){const result=arithmetic(query);if(result!==null)return{tool:"calculator",result};const sources=await webSearch(query);return{tool:"web_search",sources}}
